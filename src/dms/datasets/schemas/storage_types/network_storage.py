schema = {
    "type": "object",
    "properties": {
        "base_path": {
            "type": "string",
            "title": "Base Path",
            "help_text": "The path that should be used as a prefix to every resource",
        },
    },
    "required": ["base_path"],
}
